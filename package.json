{
  "name": "defi-erc4626-hardhat",
  "private": true,
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "slither": "slither . --config-file slither.config.json",
    "semgrep": "semgrep scan --config semgrep/solidity.yml contracts",
    "echidna": "bash scripts/run-echidna.sh",
    "fuzz": "forge test -vv"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.0",
    "@types/node": "^20.12.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "@5.4.0"
  }
}
